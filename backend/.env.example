# ========================================
# MySellGuid Backend Configuration
# ========================================
# Copy this file to .env and update the values
# For local development, you only need to configure Database and Redis.
# Optional services (AWS, OpenAI, Firebase) can be added later.

# ========================================
# APPLICATION
# ========================================
NODE_ENV=development
PORT=3000
API_PREFIX=api

# ========================================
# DATABASE (PostgreSQL with PostGIS) - REQUIRED
# ========================================
# Option 1: Local PostgreSQL
# Install: apt-get install postgresql postgis
# Then: service postgresql start && createdb mysellguid
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=mysellguid

# Option 2: Cloud PostgreSQL (Recommended for quick setup)
# Free tier with PostGIS: https://supabase.com or https://neon.tech
# Uncomment and update these:
# DATABASE_HOST=your-project.supabase.co
# DATABASE_PORT=5432
# DATABASE_USER=postgres
# DATABASE_PASSWORD=your-secure-password
# DATABASE_NAME=postgres

# ========================================
# REDIS (Caching & Background Jobs) - REQUIRED
# ========================================
# Option 1: Local Redis
# Install: apt-get install redis-server
# Then: redis-server --daemonize yes
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Option 2: Cloud Redis (Recommended for quick setup)
# Free tier: https://upstash.com or https://redis.com/try-free
# Uncomment and update these:
# REDIS_HOST=your-redis.upstash.io
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# ========================================
# JWT AUTHENTICATION - REQUIRED
# ========================================
# IMPORTANT: Change these secrets in production!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-refresh-secret-key
JWT_REFRESH_EXPIRES_IN=30d

# ========================================
# AWS S3 (Image Storage) - OPTIONAL
# ========================================
# Only needed if you want to upload images
# For now, using placeholder image URLs works fine
AWS_REGION=il-central-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
S3_BUCKET_NAME=mysellguid-images
S3_ENDPOINT=

# ========================================
# OPENAI (AI Features) - OPTIONAL
# ========================================
# Only needed for AI-powered features
# Get API key: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key

# ========================================
# APIFY (Social Media Scraping) - OPTIONAL
# ========================================
# Only needed for Instagram/Facebook scraping
# Get API token: https://apify.com
APIFY_API_TOKEN=apify_api_your-token

# ========================================
# FIREBASE (Push Notifications) - OPTIONAL
# ========================================
# Only needed for push notifications
# See FIREBASE_SETUP.md for detailed setup instructions
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY=your-private-key
FIREBASE_CLIENT_EMAIL=your-client-email

# ========================================
# APPLICATION SETTINGS
# ========================================
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp
DEFAULT_SEARCH_RADIUS=5000
MAX_SEARCH_RADIUS=50000

# ========================================
# RATE LIMITING
# ========================================
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# ========================================
# LOGGING
# ========================================
LOG_LEVEL=debug
