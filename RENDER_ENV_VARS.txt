# ================================================
# RENDER ENVIRONMENT VARIABLES - COPY THESE TO RENDER
# ================================================
# Go to: https://dashboard.render.com/web/srv-d4n9gire5dus738vdcug/env
# Add each key-value pair below
# ================================================

# REQUIRED - You MUST set these:
# ------------------------------------------------

NODE_ENV=production
PORT=10000

# DATABASE_URL - Get from Render PostgreSQL dashboard
# Go to: https://dashboard.render.com/d/dpg-d4n9ef24d50c73fa37g0-a
# Copy "Internal Database URL" and paste here:
DATABASE_URL=<PASTE_YOUR_INTERNAL_DATABASE_URL_HERE>

# REDIS_URL - Get from Render Redis dashboard  
# Go to: https://dashboard.render.com → mysellguid-redis
# Copy "Internal Redis URL" and paste here:
REDIS_URL=<PASTE_YOUR_INTERNAL_REDIS_URL_HERE>

# JWT Secrets - IMPORTANT: Generate secure random strings!
# Run this in PowerShell to generate:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=<GENERATE_64_CHAR_RANDOM_STRING>
JWT_REFRESH_SECRET=<GENERATE_ANOTHER_64_CHAR_RANDOM_STRING>
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# ------------------------------------------------
# OPTIONAL - These have defaults but you can customize:
# ------------------------------------------------

# API Configuration
API_PREFIX=api

# CORS - Already configured in code, but you can override
# CORS_ORIGINS=https://mysellguid.com,https://mysellguid-web.vercel.app

# Upload Configuration (local storage for free tier)
STORAGE_TYPE=local
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=warn

# ------------------------------------------------
# FUTURE - Add these when you need the features:
# ------------------------------------------------

# Google Maps API (for geocoding - optional for MVP)
# GOOGLE_MAPS_API_KEY=your_key_here

# Firebase (for push notifications)
# FIREBASE_PROJECT_ID=your_project_id
# FIREBASE_PRIVATE_KEY=your_private_key
# FIREBASE_CLIENT_EMAIL=your_client_email

# OpenAI (for AI features)
# OPENAI_API_KEY=your_key_here

# Apify (for web scraping)
# APIFY_API_TOKEN=your_token_here

# ================================================
# QUICK SETUP COMMANDS (run in PowerShell)
# ================================================
#
# Generate JWT_SECRET:
# node -e "console.log('JWT_SECRET=' + require('crypto').randomBytes(32).toString('hex'))"
#
# Generate JWT_REFRESH_SECRET:
# node -e "console.log('JWT_REFRESH_SECRET=' + require('crypto').randomBytes(32).toString('hex'))"
#
# ================================================

